dash==2.14.2
dash-bootstrap-components==1.5.0
pandas==2.1.4
plotly==5.18.0
gunicorn==21.2.0
```

### 2. **Rename your main file**
Your main app file should be named `app.py` (it looks like it might already be named that based on the code structure).

### 3. **Optional: Create a `Procfile`** (for some platforms)
```
web: gunicorn app:server
